package me.supcheg.javafile.type;

/// The unbounded wildcard type argument `?`.
///
/// A singleton enum with a single constant, [#INSTANCE]; obtain it via
/// [Types#unbounded()].
public enum UnboundedTypeArg implements TypeArg {
    INSTANCE
}
